import styled from "styled-components";
import tw from "twin.macro";

export const SegmentMain = styled.div`
  ${tw`flex ml-5 mb-5 px-1 pb-1 pt-2 flex-col w-auto`}
`;

export const SegmnetContainer = styled.div`
  ${tw`border-4 border-blue-500 rounded-2xl`}
  ${tw`flex flex-row flex-wrap items-center w-full h-full`}
`;
